# ===========================================
# MantelBuddy - Omgevingsvariabelen
# ===========================================
# Kopieer dit bestand naar .env.local en vul de waarden in:
#   cp .env.example .env.local
# ===========================================

# --- Database (Supabase PostgreSQL) ---
DATABASE_URL="postgresql://postgres:[WACHTWOORD]@db.[PROJECT-ID].supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[WACHTWOORD]@db.[PROJECT-ID].supabase.co:5432/postgres"

# --- NextAuth ---
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""  # Genereer met: openssl rand -base64 32

# --- AI Chat (Ger) ---
# Maak een key aan op: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=""

# --- Twilio (WhatsApp) --- (optioneel)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM=""
TWILIO_MESSAGING_SERVICE_SID=""
TWILIO_CONTENT_SID_MAIN_MENU=""
TWILIO_CONTENT_SID_TEST_ANSWER=""
TWILIO_CONTENT_SID_ONBOARDING=""

# --- E-mail (SMTP) --- (optioneel)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASSWORD=""
EMAIL_FROM="MantelBuddy <noreply@mantelbuddy.nl>"

# --- Redis (sessie-opslag) --- (optioneel)
# REDIS_URL=""

# --- Publieke URL --- (optioneel, voor productie)
# NEXT_PUBLIC_APP_URL="https://jouw-domein.nl"

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DATABASE_URL")
  directUrl  = env("DIRECT_URL")
  extensions = [vector]
}

model User {
  id             String              @id @default(cuid())
  email          String              @unique
  name           String?
  password       String?
  emailVerified  DateTime?
  image          String?
  role           UserRole            @default(CAREGIVER)
  gemeenteNaam   String?             // Alleen voor GEMEENTE_ADMIN: naam van de gemeente
  gemeenteRollen String[]            // Sub-rollen binnen gemeente: COMMUNICATIE, HULPBRONNEN, BELEID
  isActive       Boolean             @default(true)
  adminNotities  String?
  sessionVersion Int                 @default(1) // Incrementeert bij elke login om oude sessies te invalideren
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @updatedAt
  accounts       Account[]
  caregiver      Caregiver?
  mantelBuddy    MantelBuddy?
  magicLinks     MagicLinkToken[]
  orgMember      OrganisationMember?
  sessions       Session[]
  berichten      Bericht[]

  @@index([gemeenteNaam])
  @@index([role])
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@index([userId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model InviteToken {
  id        String    @id @default(cuid())
  email     String
  token     String    @unique
  gemeenteNaam String?
  gemeenteRollen String[]
  invitedBy String
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())
}

model MagicLinkToken {
  id        String    @id @default(cuid())
  token     String    @unique
  userId    String
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())
  user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Caregiver {
  id                        String                  @id @default(cuid())
  userId                    String                  @unique
  dateOfBirth               DateTime?
  phoneNumber               String?              @unique
  postalCode                String?
  city                      String?
  careRecipient             String?
  careHoursPerWeek          Int?
  careSince                 DateTime?
  intakeCompleted           Boolean                 @default(false)
  onboardedAt               DateTime?
  createdAt                 DateTime                @default(now())
  updatedAt                 DateTime                @updatedAt
  careRecipientCity         String?
  careRecipientMunicipality String?
  careRecipientName         String?
  careRecipientNeighborhood String?
  careRecipientStreet       String?
  careRecipientLatitude     Float?
  careRecipientLongitude    Float?
  municipality              String?
  neighborhood              String?
  profileCompleted          Boolean                 @default(false)
  street                    String?
  gelezenNieuws             Json?                   // Array van gelezen gemeente-nieuws IDs
  // AVG consent tracking
  privacyConsentAt          DateTime?               // Tijdstip privacy toestemming
  dataProcessingConsentAt   DateTime?               // Tijdstip gegevensverwerking toestemming
  healthDataConsentAt       DateTime?               // Tijdstip gezondheidsdata toestemming (Art. 9 AVG)
  belastbaarheidTests       BelastbaarheidTest[]
  calendarEvents            CalendarEvent[]
  user                      User                    @relation(fields: [userId], references: [id], onDelete: Cascade)
  organisationLinks         CaregiverOrganisation[]
  helpRequests              HelpRequest[]
  intakeResponses           IntakeResponse[]
  monthlyCheckIns           MonthlyCheckIn[]
  tasks                     Task[]
  // MantelBuddy relaties
  buddyMatches              BuddyMatch[]
  uitgezetteTaken           BuddyTaak[]
  gegevenBeoordelingen      BuddyBeoordeling[]
  // Favorieten
  favorieten                Favoriet[]
  // Aandoening zorgvrager & voorkeuren
  aandoening                String?                 // Primaire aandoening van zorgvrager (tag slug)
  voorkeuren                GebruikerVoorkeur[]
  weekKaarten               WeekKaart[]
  // Profiel uitbreiding (Iteratie A)
  woonsituatie              String?                 // "samen" | "dichtbij" | "op-afstand"
  werkstatus                String?                 // "fulltime" | "parttime" | "niet-werkend" | "student" | "gepensioneerd"

  @@index([municipality])
  @@index([careRecipientMunicipality])
}

model IntakeCategory {
  id          String           @id @default(cuid())
  name        String
  description String?
  order       Int              @default(0)
  questions   IntakeQuestion[]
}

model IntakeQuestion {
  id          String           @id @default(cuid())
  categoryId  String
  question    String
  description String?
  type        QuestionType     @default(SCALE)
  options     Json?
  order       Int              @default(0)
  isRequired  Boolean          @default(true)
  category    IntakeCategory   @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  responses   IntakeResponse[]
}

model IntakeResponse {
  id          String         @id @default(cuid())
  caregiverId String
  questionId  String
  value       String
  score       Int?
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt
  caregiver   Caregiver      @relation(fields: [caregiverId], references: [id], onDelete: Cascade)
  question    IntakeQuestion @relation(fields: [questionId], references: [id], onDelete: Cascade)

  @@unique([caregiverId, questionId])
}

model MonthlyCheckIn {
  id                  String    @id @default(cuid())
  caregiverId         String
  month               DateTime
  overallWellbeing    Int?
  physicalHealth      Int?
  emotionalHealth     Int?
  workLifeBalance     Int?
  supportSatisfaction Int?
  highlights          String?
  challenges          String?
  needsHelp           String?
  completedAt         DateTime?
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt
  caregiver           Caregiver @relation(fields: [caregiverId], references: [id], onDelete: Cascade)

  @@unique([caregiverId, month])
}

model Task {
  id              String       @id @default(cuid())
  caregiverId     String
  title           String
  description     String?
  category        TaskCategory @default(OTHER)
  priority        TaskPriority @default(MEDIUM)
  status          TaskStatus   @default(TODO)
  dueDate         DateTime?
  completedAt     DateTime?
  isSuggested     Boolean      @default(false)
  suggestedReason String?
  createdAt       DateTime     @default(now())
  updatedAt       DateTime     @updatedAt
  caregiver       Caregiver    @relation(fields: [caregiverId], references: [id], onDelete: Cascade)
}

model Resource {
  id          String       @id @default(cuid())
  title       String
  description String
  content     String?
  url         String?
  type        ResourceType
  category    String
  isNational  Boolean      @default(true)
  postalCodes String[]
  city        String?
  isActive    Boolean      @default(true)
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

model Organisation {
  id           String                  @id @default(cuid())
  name         String
  description  String?
  logo         String?
  email        String?
  phone        String?
  website      String?
  address      String?
  postalCode   String?
  city         String?
  isActive     Boolean                 @default(true)
  createdAt    DateTime                @default(now())
  updatedAt    DateTime                @updatedAt
  caregivers   CaregiverOrganisation[]
  helpRequests HelpRequest[]
  members      OrganisationMember[]
}

model OrganisationMember {
  id             String        @id @default(cuid())
  userId         String        @unique
  organisationId String
  role           OrgMemberRole @default(MEMBER)
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  organisation   Organisation  @relation(fields: [organisationId], references: [id], onDelete: Cascade)
  user           User          @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model CaregiverOrganisation {
  id             String       @id @default(cuid())
  caregiverId    String
  organisationId String
  shareWellbeing Boolean      @default(false)
  shareNeeds     Boolean      @default(false)
  shareProgress  Boolean      @default(false)
  joinedAt       DateTime     @default(now())
  caregiver      Caregiver    @relation(fields: [caregiverId], references: [id], onDelete: Cascade)
  organisation   Organisation @relation(fields: [organisationId], references: [id], onDelete: Cascade)

  @@unique([caregiverId, organisationId])
}

model Notification {
  id           String           @id @default(cuid())
  userId       String
  type         NotificationType
  title        String
  message      String
  link         String?
  isRead       Boolean          @default(false)
  readAt       DateTime?
  scheduledFor DateTime?
  sentAt       DateTime?
  createdAt    DateTime         @default(now())

  @@index([userId])
  @@index([userId, isRead])
}

model HelpRequest {
  id             String        @id @default(cuid())
  caregiverId    String
  title          String
  description    String
  category       HelpCategory
  urgency        HelpUrgency   @default(NORMAL)
  status         HelpStatus    @default(OPEN)
  organisationId String?
  response       String?
  respondedBy    String?
  respondedAt    DateTime?
  resolvedAt     DateTime?
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  caregiver      Caregiver     @relation(fields: [caregiverId], references: [id], onDelete: Cascade)
  organisation   Organisation? @relation(fields: [organisationId], references: [id], onDelete: SetNull)

  @@index([caregiverId])
}

model CalendarEvent {
  id              String    @id @default(cuid())
  caregiverId     String
  title           String
  description     String?
  location        String?
  startTime       DateTime
  endTime         DateTime?
  isAllDay        Boolean   @default(false)
  isRecurring     Boolean   @default(false)
  recurrenceRule  String?
  eventType       EventType @default(OTHER)
  reminderMinutes Int?
  color           String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  caregiver       Caregiver @relation(fields: [caregiverId], references: [id], onDelete: Cascade)
}

model PasswordResetToken {
  id        String   @id @default(cuid())
  email     String
  token     String   @unique
  expires   DateTime
  createdAt DateTime @default(now())
}

model BelastbaarheidTest {
  id                   String                   @id @default(cuid())
  caregiverId          String?
  voornaam             String
  email                String
  postcode             String?
  huisnummer           String?
  straat               String?
  woonplaats           String?
  gemeente             String?
  totaleBelastingScore Int                      @default(0)
  belastingNiveau      BelastingNiveau          @default(LAAG)
  totaleZorguren       Int                      @default(0)
  isCompleted          Boolean                  @default(false)
  completedAt          DateTime?
  createdAt            DateTime                 @default(now())
  updatedAt            DateTime                 @updatedAt
  alarmLogs            AlarmLog[]
  antwoorden           BelastbaarheidAntwoord[]
  rapporten            BelastbaarheidRapport[]
  caregiver            Caregiver?               @relation(fields: [caregiverId], references: [id], onDelete: SetNull)
  taakSelecties        ZorgtaakSelectie[]

  @@index([caregiverId])
  @@index([caregiverId, completedAt])
  @@index([gemeente])
  @@index([isCompleted, gemeente])
  @@index([completedAt])
}

model BelastbaarheidAntwoord {
  id         String             @id @default(cuid())
  testId     String
  vraagId    String
  vraagTekst String
  antwoord   String
  score      Int
  gewicht    Float              @default(1.0)
  createdAt  DateTime           @default(now())
  test       BelastbaarheidTest @relation(fields: [testId], references: [id], onDelete: Cascade)

  @@unique([testId, vraagId])
}

model ZorgtaakSelectie {
  id             String             @id @default(cuid())
  testId         String
  taakId         String
  taakNaam       String
  isGeselecteerd Boolean            @default(false)
  urenPerWeek    Int?
  moeilijkheid   Moeilijkheid?
  createdAt      DateTime           @default(now())
  test           BelastbaarheidTest @relation(fields: [testId], references: [id], onDelete: Cascade)

  @@unique([testId, taakId])
}

model Zorgorganisatie {
  id                    String              @id @default(cuid())
  naam                  String
  beschrijving          String?
  type                  ZorgorganisatieType
  telefoon              String?
  email                 String?
  website               String?
  adres                 String?
  postcode              String?
  woonplaats            String?             // Vestigingsplaats van de organisatie
  gemeente              String?             // Dekkingsgebied gemeente (null bij landelijk/provincie)
  provincie             String?             // Dekkingsgebied provincie
  dekkingNiveau         String              @default("GEMEENTE") // LANDELIJK, PROVINCIE, GEMEENTE, WOONPLAATS, WIJK
  dekkingWoonplaatsen   Json?               // Array van woonplaatsen binnen gemeente (null = hele gemeente)
  dekkingWijken         Json?               // Array van wijknamen binnen gemeente (null = hele gemeente)
  isActief              Boolean             @default(true)
  createdAt             DateTime            @default(now())
  updatedAt             DateTime            @updatedAt
  openingstijden        String?
  zichtbaarBijGemiddeld Boolean             @default(false)
  zichtbaarBijHoog      Boolean             @default(true)
  zichtbaarBijLaag      Boolean             @default(false)
  aanmeldprocedure      String?
  dienst                String?
  doelgroep             String?
  kosten                String?
  locatieOmschrijving   String?
  onderdeelTest         String?             // Categorie: Persoonlijke verzorging, Ondersteuning, Vervangende mantelzorg, etc.
  soortHulp             String?
  voorwaarden           String?
  eersteStap            String?             // "Bel en vraag naar een intake-gesprek" — eerste concrete actie
  verwachtingTekst      String?             // "Ze komen bij je thuis kijken" — wat kan je verwachten
  verschijntIn          String[]            // Array van plekken waar dit item verschijnt (voor cross-referencing)
  routeLabel            String?             // Wet/route label: Wmo, Zvw, Wlz (voor display op hulpkaarten)
  bronLabel             String?             // Bronlabel: Landelijk, Gemeente, Zvw, Wlz, Overig
  zorgverzekeraar       Boolean             @default(false) // Tag: valt onder zorgverzekeraar

  // Vector embedding voor semantische zoek (pgvector, 1536 dimensies)
  // Gegenereerd door OpenAI text-embedding-3-small
  embedding             Unsupported("vector(1536)")?

  // Validatie-relatie
  validaties            HulpbronValidatie[]
  laatsteValidatie       DateTime?           // Timestamp van laatste validatie-check
  validatieStatus        String?             // GELDIG, WAARSCHUWING, ONGELDIG, ONBEKEND
  bronType               String?             // Hoe gevonden: AI_ZOEKER, HANDMATIG, IMPORT, SOCIALE_KAART

  @@index([isActief, gemeente])
  @@index([isActief, onderdeelTest])
  @@index([gemeente])
  @@index([validatieStatus])
}

// Validatieresultaten voor hulpbronnen — wekelijkse controle of informatie nog klopt
model HulpbronValidatie {
  id                String                  @id @default(cuid())
  zorgorganisatieId String
  status            HulpbronValidatieStatus
  websiteBereikbaar Boolean?
  websiteStatusCode Int?
  contentKlopt      Boolean?
  telefoonGeldig    Boolean?
  opmerkingen       String?                 @db.Text
  aiSamenvatting    String?                 @db.Text
  gecontroleerd     DateTime                @default(now())
  zorgorganisatie   Zorgorganisatie         @relation(fields: [zorgorganisatieId], references: [id], onDelete: Cascade)

  @@index([zorgorganisatieId])
  @@index([status])
  @@index([gecontroleerd])
}

model BelastbaarheidRapport {
  id              String             @id @default(cuid())
  testId          String
  samenvatting    String
  aandachtspunten Json
  aanbevelingen   Json
  lokaleHulp      Json
  pdfUrl          String?
  createdAt       DateTime           @default(now())
  test            BelastbaarheidTest @relation(fields: [testId], references: [id], onDelete: Cascade)
}

model AlarmLog {
  id              String             @id @default(cuid())
  testId          String
  type            AlarmType
  beschrijving    String
  urgentie        AlarmUrgentie      @default(MEDIUM)
  isAfgehandeld   Boolean            @default(false)
  afgehandeldOp   DateTime?
  afgehandeldDoor String?
  notitie         String?
  createdAt       DateTime           @default(now())
  test            BelastbaarheidTest @relation(fields: [testId], references: [id], onDelete: Cascade)
}

// ============================================
// ARTIKELEN & CONTENT MANAGEMENT
// ============================================

model Artikel {
  id              String          @id @default(cuid())
  titel           String
  beschrijving    String
  inhoud          String?         // Rich text inhoud (optioneel, voor uitgebreide artikelen)
  url             String?         // Externe link
  bron            String?         // Bronnaam (bijv. "MantelzorgNL")
  emoji           String?
  categorie       String          // Slug: praktische-tips, zelfzorg, rechten, financieel, hulpmiddelen-producten
  subHoofdstuk    String?         // Sub-chapter slug binnen categorie (bijv. "dagelijks-organiseren", "overbelasting-herkennen")
  type            ArtikelType     @default(ARTIKEL)
  status          ArtikelStatus   @default(CONCEPT)
  bronLabel       String?         // Bronlabel: Landelijk, Gemeente (Wmo), Zorgverzekeraar (Zvw), Wlz, Overig
  gemeente        String?         // Alleen voor gemeente-nieuws
  publicatieDatum DateTime?
  sorteerVolgorde Int             @default(0)
  isActief        Boolean         @default(true)
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt
  aangemaaaktDoor String?         // User ID van de maker

  // Tags (many-to-many via ArtikelTag)
  tags            ArtikelTag[]

  // Bronvermeldingen en curator reviews
  bronnen         ArtikelBron[]
  curatorReviews  CuratorReview[]

  // Vector embedding voor semantische zoek (pgvector, 1536 dimensies)
  // Gegenereerd door OpenAI text-embedding-3-small
  embedding             Unsupported("vector(1536)")?

  @@index([categorie, isActief])
  @@index([type, isActief])
  @@index([gemeente])
  @@index([subHoofdstuk])
}

model AuditLog {
  id          String   @id @default(cuid())
  userId      String
  actie       String   // CREATE, UPDATE, DELETE, LOGIN, etc.
  entiteit    String   // User, Artikel, Zorgorganisatie, etc.
  entiteitId  String?
  details     Json?    // Extra informatie over de wijziging
  ipAdres     String?
  createdAt   DateTime @default(now())

  @@index([userId])
  @@index([entiteit, entiteitId])
  @@index([createdAt])
}

// ============================================
// FAVORIETEN
// ============================================

model Favoriet {
  id            String       @id @default(cuid())
  caregiverId   String
  type          FavorietType
  itemId        String       // Uniek ID: voor HULP = slug van naam+gemeente, voor INFORMATIE = categorie-id
  titel         String
  beschrijving  String?
  categorie     String?      // Groepering (bijv. "Persoonlijke verzorging", "Praktische tips")
  url           String?
  telefoon      String?
  icon          String?      // Emoji icoon
  isVoltooid    Boolean      @default(false)
  voltooitOp    DateTime?
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @updatedAt
  caregiver     Caregiver    @relation(fields: [caregiverId], references: [id], onDelete: Cascade)

  @@unique([caregiverId, type, itemId])
  @@index([caregiverId])
}

// ============================================
// MANTELBUDDY VRIJWILLIGERS
// ============================================

model MantelBuddy {
  id                String              @id @default(cuid())

  // Persoonlijke gegevens
  voornaam          String
  achternaam        String
  email             String              @unique
  telefoon          String
  postcode          String
  woonplaats        String

  // Locatie (geocoded van postcode)
  latitude          Float?
  longitude         Float?
  maxReisafstand    Int?                // Max km bereid te reizen (standaard 10)

  // Wat wil de buddy doen
  hulpvormen        String[]            // gesprek, boodschappen, vervoer, klusjes, oppas, administratie
  beschikbaarheid   BuddyBeschikbaarheid @default(BEIDE)
  motivatie         String?
  ervaring          String?

  // Status & verificatie
  status            BuddyStatus         @default(AANGEMELD)
  vogGoedgekeurd    Boolean             @default(false)
  trainingVoltooid  Boolean             @default(false)
  isActief          Boolean             @default(false)

  // Scores & beoordelingen
  gemiddeldeScore   Float               @default(0)
  aantalBeoordelingen Int               @default(0)
  betrouwbaarheidScore Float            @default(0)
  aantalTakenVoltooid Int               @default(0)
  aantalTakenGeannuleerd Int            @default(0)

  // Relaties
  userId            String?             @unique
  user              User?               @relation(fields: [userId], references: [id])
  matches           BuddyMatch[]
  taakReacties      BuddyTaakReactie[]
  ontvangenBeoordelingen BuddyBeoordeling[] @relation("BeoordeeldeBuddy")
  gegevenBeoordelingen   BuddyBeoordeling[] @relation("BeoordelendePersoon")

  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
}

model BuddyMatch {
  id                String              @id @default(cuid())
  buddyId           String
  caregiverId       String

  // Status van de match
  status            MatchStatus         @default(VOORGESTELD)

  // Type match
  isVasteBuddy      Boolean             @default(false)  // vast maatje vs eenmalig

  // Notities
  notities          String?

  // Relaties
  buddy             MantelBuddy         @relation(fields: [buddyId], references: [id], onDelete: Cascade)
  caregiver         Caregiver           @relation(fields: [caregiverId], references: [id], onDelete: Cascade)
  taakReacties      BuddyTaakReactie[]
  berichten         Bericht[]

  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt

  @@unique([buddyId, caregiverId])
}

model BuddyTaak {
  id                String              @id @default(cuid())
  caregiverId       String

  // Taak details
  titel             String
  beschrijving      String?
  categorie         BuddyTaakCategorie

  // Wanneer
  datum             DateTime?
  tijdstip          String?             // bijv. "ochtend", "middag", "avond"
  isFlexibel        Boolean             @default(true)

  // Status
  status            TaakUitzetStatus    @default(OPEN)

  // Koppeling
  toegewezenAan     String?             // MantelBuddy id

  // Relaties
  caregiver         Caregiver           @relation(fields: [caregiverId], references: [id], onDelete: Cascade)
  reacties          BuddyTaakReactie[]

  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt
}

model BuddyTaakReactie {
  id                String              @id @default(cuid())
  taakId            String
  buddyId           String
  matchId           String?

  // Status
  status            ReactieStatus       @default(INTERESSE)

  // Bericht van de buddy
  bericht           String?

  // Relaties
  taak              BuddyTaak           @relation(fields: [taakId], references: [id], onDelete: Cascade)
  buddy             MantelBuddy         @relation(fields: [buddyId], references: [id], onDelete: Cascade)
  match             BuddyMatch?         @relation(fields: [matchId], references: [id])

  createdAt         DateTime            @default(now())
  updatedAt         DateTime            @updatedAt

  @@unique([taakId, buddyId])
}

model BuddyBeoordeling {
  id                String              @id @default(cuid())

  // Wie beoordeelt wie
  buddyId           String              // De beoordeelde MantelBuddy
  beoordeeldDoorId  String?             // Optioneel: als dit een buddy is die terugkoppelt
  caregiverId       String?             // De mantelzorger die beoordeelt

  // Scores (1-5 sterren)
  algemeenScore     Int                 // Algemene ervaring
  betrouwbaarheidScore Int?             // Kwam op tijd, deed wat afgesproken
  vriendelijkheidScore Int?             // Was vriendelijk en behulpzaam
  kwaliteitScore    Int?                // Kwaliteit van de hulp

  // Feedback
  positieveFeedback String?
  verbeterpunten    String?

  // Zichtbaarheid
  isAnoniem         Boolean             @default(false)
  isZichtbaar       Boolean             @default(true)

  // Relaties
  buddy             MantelBuddy         @relation("BeoordeeldeBuddy", fields: [buddyId], references: [id], onDelete: Cascade)
  beoordeeldDoor    MantelBuddy?        @relation("BeoordelendePersoon", fields: [beoordeeldDoorId], references: [id], onDelete: SetNull)
  caregiver         Caregiver?          @relation(fields: [caregiverId], references: [id], onDelete: SetNull)

  createdAt         DateTime            @default(now())

  @@index([buddyId])
}

model Bericht {
  id                String              @id @default(cuid())
  matchId           String
  afzenderId        String              // User id

  inhoud            String
  isGelezen         Boolean             @default(false)
  gelezenOp         DateTime?

  match             BuddyMatch          @relation(fields: [matchId], references: [id], onDelete: Cascade)
  afzender          User                @relation(fields: [afzenderId], references: [id], onDelete: Cascade)

  createdAt         DateTime            @default(now())

  @@index([matchId])
  @@index([matchId, createdAt])
}

// ============================================
// ENUMS
// ============================================

enum FavorietType {
  HULP        // Hulporganisatie van hulpvragen pagina
  INFORMATIE  // Leren/informatie item van informatie pagina
}

enum BuddyBeschikbaarheid {
  EENMALIG          // Alleen losse taken
  VAST              // Alleen vast maatje
  BEIDE             // Allebei
}

enum BuddyStatus {
  AANGEMELD         // Net aangemeld
  IN_BEHANDELING    // Kennismakingsgesprek gepland
  VOG_AANGEVRAAGD   // VOG in behandeling
  GOEDGEKEURD       // Volledig goedgekeurd, kan matchen
  INACTIEF          // Tijdelijk niet beschikbaar
  AFGEWEZEN         // Aanmelding afgewezen
}

enum MatchStatus {
  VOORGESTELD       // Match voorgesteld aan beide partijen
  BUDDY_AKKOORD     // Buddy heeft geaccepteerd
  CAREGIVER_AKKOORD // Mantelzorger heeft geaccepteerd
  ACTIEF            // Match is actief
  GEPAUZEERD        // Tijdelijk gepauzeerd
  BEEINDIGD         // Match is beëindigd
}

enum BuddyTaakCategorie {
  GESPREK           // Koffiedrinken, luisterend oor
  BOODSCHAPPEN      // Supermarkt, apotheek
  VERVOER           // Naar afspraken rijden
  KLUSJES           // Kleine reparaties, tuin
  OPPAS             // Bij zorgvrager zijn
  ADMINISTRATIE     // Papierwerk, formulieren
  OVERIG            // Anders
}

enum TaakUitzetStatus {
  OPEN              // Taak staat open
  REACTIES          // Er zijn reacties
  TOEGEWEZEN        // Iemand is toegewezen
  VOLTOOID          // Taak is voltooid
  GEANNULEERD       // Taak geannuleerd
}

enum ReactieStatus {
  INTERESSE         // Buddy heeft interesse getoond
  GEACCEPTEERD      // Mantelzorger heeft buddy gekozen
  AFGEWEZEN         // Niet gekozen
  VOLTOOID          // Taak is voltooid door deze buddy
}

enum UserRole {
  CAREGIVER
  BUDDY             // Nieuwe rol voor MantelBuddy vrijwilligers
  ORG_MEMBER
  ORG_ADMIN
  GEMEENTE_ADMIN    // Gemeenteportaal medewerker
  ADMIN
}

enum QuestionType {
  SCALE
  MULTIPLE_CHOICE
  YES_NO
  TEXT
}

enum TaskCategory {
  SELF_CARE
  ADMINISTRATION
  APPOINTMENTS
  SOCIAL
  HEALTH
  OTHER
}

enum TaskPriority {
  LOW
  MEDIUM
  HIGH
}

enum TaskStatus {
  TODO
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum ResourceType {
  ARTICLE
  LOCAL_SERVICE
  NATIONAL_SERVICE
  TIP
  VIDEO
  TOOL
}

enum OrgMemberRole {
  MEMBER
  ADMIN
}

enum NotificationType {
  CHECK_IN_REMINDER
  TASK_REMINDER
  HELP_REQUEST_UPDATE
  TIP
  SYSTEM
  BUDDY_REACTIE           // Buddy reageert op hulpvraag
  REACTIE_GEACCEPTEERD    // Mantelzorger accepteert buddy
  REACTIE_AFGEWEZEN       // Mantelzorger wijst buddy af
  BUDDY_BERICHT           // Nieuw chatbericht
}

enum HelpCategory {
  RESPITE_CARE
  EMOTIONAL_SUPPORT
  PRACTICAL_HELP
  FINANCIAL_ADVICE
  INFORMATION
  OTHER
}

enum HelpUrgency {
  LOW
  NORMAL
  HIGH
  URGENT
}

enum HelpStatus {
  OPEN
  IN_PROGRESS
  RESPONDED
  RESOLVED
  CLOSED
}

enum EventType {
  CARE_TASK
  APPOINTMENT
  SELF_CARE
  SOCIAL
  WORK
  OTHER
}

enum BelastingNiveau {
  LAAG
  GEMIDDELD
  HOOG
}

enum Moeilijkheid {
  MAKKELIJK
  GEMIDDELD
  MOEILIJK
  ZEER_MOEILIJK
}

enum ZorgorganisatieType {
  GEMEENTE
  THUISZORG
  MANTELZORGSTEUNPUNT
  RESPIJTZORG
  DAGBESTEDING
  HUISARTS
  SOCIAAL_WIJKTEAM
  VRIJWILLIGERS
  OVERIG
  LANDELIJK
}

enum HulpbronValidatieStatus {
  GELDIG
  WAARSCHUWING
  ONGELDIG
  ONBEKEND
}

enum AlarmType {
  HOGE_BELASTING
  KRITIEKE_COMBINATIE
  VEEL_ZORGUREN
  EMOTIONELE_NOOD
  SOCIAAL_ISOLEMENT
  FYSIEKE_KLACHTEN
}

enum AlarmUrgentie {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum ArtikelType {
  ARTIKEL           // Regulier informatief artikel
  GEMEENTE_NIEUWS   // Gemeentenieuws
  TIP               // Korte tip
}

enum ArtikelStatus {
  VOORSTEL          // Idee/voorstel uit hiaten-analyse of online zoeken
  CONCEPT           // AI-gegenereerd concept, klaar voor herschrijven
  HERSCHREVEN       // Herschreven op B1-niveau, klaar voor verrijking
  VERRIJKT          // Verrijkt met extra tips/FAQ/bronnen, klaar voor publicatie
  GEPUBLICEERD      // Live voor gebruikers
  GEARCHIVEERD      // Niet meer zichtbaar
}

// ============================================
// CONTENT TAGS & VOORKEUREN
// ============================================

model ContentTag {
  id           String       @id @default(cuid())
  type         TagType
  slug         String       @unique
  naam         String
  beschrijving String?
  emoji        String?
  groep        String?      // Dimensie: "zorgthema" | "relatie" | "weekinvulling" | "wonen" | "zorgduur" | "extra" | "rouw" | "onderwerp"
  synoniemen   String[]     @default([]) // Synoniemen voor zoeken, bijv. ["PGB", "persoonsgebonden budget"]
  volgorde     Int          @default(0)
  isActief     Boolean      @default(true)
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt
  artikelTags  ArtikelTag[]

  @@index([type, isActief])
  @@index([groep])
}

model ArtikelTag {
  id        String     @id @default(cuid())
  artikelId String
  tagId     String
  artikel   Artikel    @relation(fields: [artikelId], references: [id], onDelete: Cascade)
  tag       ContentTag @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@unique([artikelId, tagId])
  @@index([tagId])
}

model GebruikerVoorkeur {
  id          String       @id @default(cuid())
  caregiverId String
  type        VoorkeurType
  slug        String       // Verwijst naar categorie-slug of tag-slug
  createdAt   DateTime     @default(now())
  caregiver   Caregiver    @relation(fields: [caregiverId], references: [id], onDelete: Cascade)

  @@unique([caregiverId, type, slug])
  @@index([caregiverId])
}

enum TagType {
  ZORGTHEMA     // Bijv. geheugen-cognitie, lichamelijk, ernstig-ziek (was: AANDOENING)
  SITUATIE      // Bijv. werkend, partner-zorg, samenwonend
  ONDERWERP     // Bijv. financien-regelingen, respijtzorg, medicatie-behandeling
}

enum VoorkeurType {
  CATEGORIE     // Favoriete content-categorie
  TAG           // Favoriete tag (aandoening/situatie)
}

// ============================================
// CONTENT BEHEER - Alle app content in database
// ============================================

// Balanstest en Check-in vragen
model BalanstestVraag {
  id           String    @id @default(cuid())
  type         VraagType
  vraagId      String    @unique  // unieke key, bijv. "q1", "c1"
  vraagTekst   String
  beschrijving String?            // Extra uitleg of sectie header intro
  tip          String?            // Tip tekst bij de vraag
  sectie       String?            // Sectie header (bijv. "Jouw energie", "Jouw gevoel")
  opties       Json?              // Array van antwoordopties [{label, waarde, score, icon}]
  gewicht      Float    @default(1.0)
  reversed     Boolean  @default(false) // Voor check-in: is "ja" het positieve antwoord?
  isMultiSelect Boolean @default(false) // Voor check-in multi-select vragen
  emoji        String?
  volgorde     Int      @default(0)
  isActief     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([type, isActief])
}

enum VraagType {
  BALANSTEST
  CHECKIN
}

// Zorgtaak definities (gebruikt in balanstest en hulpvragen)
model Zorgtaak {
  id           String   @id @default(cuid())
  taakId       String   @unique  // unieke key, bijv. "t1", "verzorging"
  naam         String
  beschrijving String?
  categorie    String?            // Groep: "Dagelijks leven", "Organisatie", etc.
  emoji        String?
  icon         String?            // Icoon naam
  kort         String?            // Korte weergavenaam
  routeLabel   String?            // "Wmo", "Zvw", etc.
  groep        String?            // Visuele groep (bijv. "Dagelijks leven")
  volgorde     Int      @default(0)
  isActief     Boolean  @default(true)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  mappings     TaakCategorieMapping[]

  @@index([isActief])
}

// Taak-naar-categorie mappings (vertaalt diverse taaknamen naar canonical categorie)
model TaakCategorieMapping {
  id           String   @id @default(cuid())
  bronNaam     String   @unique  // Naam zoals binnenkomt (bijv. "Wassen/aankleden")
  zorgtaakId   String
  zorgtaak     Zorgtaak @relation(fields: [zorgtaakId], references: [id], onDelete: Cascade)
  bron         String?            // "WEB", "WHATSAPP" — herkomst van de mapping
  isActief     Boolean  @default(true)
  createdAt    DateTime @default(now())

  @@index([zorgtaakId])
}

// Content categorieën (leren, hulpvraag, sub-hoofdstukken, etc.)
model ContentCategorie {
  id           String            @id @default(cuid())
  type         CategorieType
  slug         String
  naam         String
  beschrijving String?
  emoji        String?
  icon         String?            // Icoon naam
  hint         String?            // Extra hint tekst
  parentId     String?
  parent       ContentCategorie?  @relation("CategorieHierarchie", fields: [parentId], references: [id], onDelete: SetNull)
  children     ContentCategorie[] @relation("CategorieHierarchie")
  metadata     Json?              // Extra data (bronLabelKleur, routeLabel, etc.)
  volgorde     Int                @default(0)
  isActief     Boolean            @default(true)
  createdAt    DateTime           @default(now())
  updatedAt    DateTime           @updatedAt

  @@unique([type, slug])
  @@index([type, isActief])
  @@index([parentId])
}

enum CategorieType {
  LEREN              // Leren hoofdcategorieën (Praktische tips, Zelfzorg, etc.)
  SUB_HOOFDSTUK      // Sub-hoofdstukken binnen leren categorieën
  HULPVRAAG          // Hulpvraag categorieën (Even vrij, Praten, etc.)
  HULP_ZORGVRAGER    // Hulp categorieën voor zorgvrager
  HULP_MANTELZORGER  // Hulp categorieën voor mantelzorger
}

// Formulier opties (dropdown/selectie opties)
model FormulierOptie {
  id           String     @id @default(cuid())
  groep        OptieGroep
  waarde       String
  label        String
  beschrijving String?
  emoji        String?
  volgorde     Int        @default(0)
  isActief     Boolean    @default(true)
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt

  @@unique([groep, waarde])
  @@index([groep, isActief])
}

enum OptieGroep {
  RELATIE                // partner, ouder, kind, etc.
  UREN_PER_WEEK          // 0-5, 5-10, etc.
  ZORGDUUR               // <1 jaar, 1-3 jaar, etc.
  UREN_BALANSTEST        // Uren opties voor balanstest (0-2, 2-4, etc.)
  BUDDY_HULPVORM         // gesprek, boodschappen, vervoer, etc.
  BUDDY_BESCHIKBAARHEID  // eenmalig, vast, beide
  CHECKIN_HULP           // Hulp opties bij check-in vraag
}

// App content (onboarding, tutorial, pagina intro's, etc.)
model AppContent {
  id           String         @id @default(cuid())
  type         AppContentType
  sleutel      String
  titel        String?
  inhoud       String?        // Hoofdtekst (markdown of plain text)
  subtekst     String?        // Secundaire tekst
  emoji        String?
  icon         String?
  afbeelding   String?        // URL naar afbeelding
  metadata     Json?          // Extra data (feature cards, knopteksten, etc.)
  volgorde     Int            @default(0)
  isActief     Boolean        @default(true)
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt

  @@unique([type, sleutel])
  @@index([type, isActief])
}

enum AppContentType {
  ONBOARDING     // Onboarding flow stappen
  TUTORIAL       // Tutorial stappen
  PAGINA_INTRO   // Pagina titels en beschrijvingen
  FEATURE_CARD   // Feature uitleg kaarten
}

// ============================================
// COACH ADVIEZEN (Ger AI coaching teksten)
// ============================================

model CoachAdvies {
  id        String   @id @default(cuid())
  sleutel   String   @unique  // bijv. "totaal.LAAG", "energie.HOOG", "taak.t1.GEMIDDELD"
  label     String?           // Leesbaar label voor admin UI
  advies    String   @db.Text // De adviestekst die Ger gebruikt
  isActief  Boolean  @default(true)
  updatedAt DateTime @updatedAt

  @@index([isActief])
}

// ============================================
// GEMEENTE BEHEER
// ============================================

model Gemeente {
  id                    String   @id @default(cuid())
  naam                  String   @unique  // Officiële gemeentenaam
  code                  String?  @unique  // CBS gemeentecode
  isActief              Boolean  @default(true)

  // Contactgegevens
  contactEmail          String?
  contactTelefoon       String?
  websiteUrl            String?
  wmoLoketUrl           String?

  // Aangepast advies per niveau
  adviesLaag            String?  @db.Text  // Advies tekst bij LAAG niveau
  adviesGemiddeld       String?  @db.Text  // Advies tekst bij GEMIDDELD niveau
  adviesHoog            String?  @db.Text  // Advies tekst bij HOOG niveau

  // Gekoppelde organisaties per belastingniveau
  organisatieLaagId     String?  // Zorgorganisatie ID voor LAAG niveau
  organisatieGemiddeldId String? // Zorgorganisatie ID voor GEMIDDELD niveau
  organisatieHoogId     String?  // Zorgorganisatie ID voor HOOG niveau

  // Aangepaste hulpbronnen links
  mantelzorgSteunpunt   String?  // URL mantelzorgsteunpunt
  mantelzorgSteunpuntNaam String? // Naam steunpunt
  respijtzorgUrl        String?
  dagopvangUrl          String?

  // Metadata
  notities              String?  @db.Text
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([isActief])
}

// ============================================
// GEMEENTE STAPPEN (Jouw stappenplan per gemeente)
// ============================================

model GemeenteStap {
  id              String          @id @default(cuid())
  gemeenteNaam    String          // Gemeente naam (of "_default" voor generiek)
  niveau          BelastingNiveau // LAAG, GEMIDDELD, HOOG
  stapNummer      Int             // 1, 2, of 3
  titel           String
  beschrijving    String?
  emoji           String?

  // Link naar organisatie (hulpkaart) OF artikel
  organisatieId   String?         // Optioneel: link naar Zorgorganisatie
  artikelId       String?         // Optioneel: link naar Artikel
  externeUrl      String?         // Optioneel: externe URL

  isActief        Boolean         @default(true)
  createdAt       DateTime        @default(now())
  updatedAt       DateTime        @updatedAt

  @@unique([gemeenteNaam, niveau, stapNummer])
  @@index([gemeenteNaam, niveau, isActief])
}

// ============================================
// SITE INSTELLINGEN (huisstijl, teksten, logo)
// ============================================

model SiteSettings {
  id        String   @id @default(cuid())
  categorie String   // "branding", "kleuren", "teksten"
  sleutel   String   @unique // bijv. "branding.appName", "kleuren.primary", "teksten.profiel.header.subtitle"
  waarde    String   @db.Text // De waarde (tekst, kleurcode, of base64 logo)
  label     String?  // Leesbaar label voor admin UI
  type      String   @default("text") // "text", "color", "image", "textarea"
  groep     String?  // Groepering in admin UI
  volgorde  Int      @default(0)
  updatedAt DateTime @updatedAt
  updatedBy String?  // User ID van de laatste wijziging

  @@index([categorie])
}

// ============================================
// WEKELIJKSE HULPKAARTEN (3 per week per mantelzorger)
// ============================================

model WeekKaart {
  id            String        @id @default(cuid())
  caregiverId   String
  weekNummer    String        // ISO week: "2026-W11"
  type          WeekKaartType // ZELFZORG, PRAKTISCH, LEREN
  titel         String
  beschrijving  String        @db.Text
  linkUrl       String?       // Optioneel: artikel of hulpbron URL
  linkLabel     String?       // "Lees artikel" of "Bel nu"
  emoji         String?       // Visueel icoon
  isVoltooid    Boolean       @default(false)
  voltooitOp    DateTime?
  bron          String        @default("REGEL") // REGEL, AI, ADMIN
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
  caregiver     Caregiver     @relation(fields: [caregiverId], references: [id], onDelete: Cascade)

  @@unique([caregiverId, weekNummer, type])
  @@index([caregiverId, weekNummer])
}

enum WeekKaartType {
  ZELFZORG
  PRAKTISCH
  LEREN
}

// ============================================
// WHATSAPP SESSIES (persistent over serverless restarts)
// ============================================

model WhatsAppSessie {
  id          String              @id @default(cuid())
  telefoonnr  String              // Telefoonnummer zonder whatsapp: prefix
  type        WhatsAppSessieType  // TEST, ONBOARDING, HULP
  stap        String              // Huidige stap in de flow
  data        Json                @default("{}") // Sessie-specifieke data (antwoorden, keuzes, etc.)
  expiresAt   DateTime            // Automatische verloopdatum (TTL)
  createdAt   DateTime            @default(now())
  updatedAt   DateTime            @updatedAt

  @@unique([telefoonnr, type]) // Max 1 sessie per type per telefoonnummer
  @@index([telefoonnr])
  @@index([expiresAt])
}

enum WhatsAppSessieType {
  TEST
  ONBOARDING
  HULP
}

// ============================================
// BRONVERMELDING (per artikel)
// ============================================

model ArtikelBron {
  id          String   @id @default(cuid())
  artikelId   String
  naam        String
  url         String?
  type        String   // "website" | "overheid" | "onderzoek" | "organisatie"
  createdAt   DateTime @default(now())
  artikel     Artikel  @relation(fields: [artikelId], references: [id], onDelete: Cascade)

  @@index([artikelId])
}

// ============================================
// CURATOR REVIEWS (AI-gestuurde content reviews)
// ============================================

model CuratorReview {
  id          String    @id @default(cuid())
  artikelId   String
  score       Int       // 0-100 kwaliteitsscore
  feedback    String    @db.Text
  issues      Json?     // Array van gevonden problemen
  reviewedAt  DateTime  @default(now())
  resolvedAt  DateTime? // Wanneer alle issues zijn opgelost
  artikel     Artikel   @relation(fields: [artikelId], references: [id], onDelete: Cascade)

  @@index([artikelId])
}
